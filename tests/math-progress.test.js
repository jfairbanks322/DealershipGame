const {test}=require('node:test'),assert=require('node:assert/strict'),{summary}=require('../lib/math-progress'),{newPlayer,pricing}=require('../lib/game'),{initializeLesson}=require('../lib/lessons');
test('math checks count each verdict, preserve retries and separate rounds',()=>{const p=newPlayer({id:'a',name:'A',badges:[]},'A','🍔','#123456'),g=initializeLesson({round:1,phase:'planning',penalty:500});for(const correct of [false,false,true,true])pricing(g,p,{id:'1',markup:100,amount:correct?2.4:0,price:correct?4.8:0});assert.deepEqual(summary(p,1),{round:{right:2,wrong:2,unclassified:0},total:{right:2,wrong:2,unclassified:0}});assert.throws(()=>pricing(g,p,{id:'2',markup:100,amount:0,price:0}));g.round=2;pricing(g,p,{id:'2',markup:100,amount:0,price:0});assert.deepEqual(summary(p,2),{round:{right:0,wrong:1,unclassified:0},total:{right:2,wrong:3,unclassified:0}});});
test('legacy checks remain unclassified and supply saves are not math checks',()=>{const p={attempts:{'1:1':3,'2:2':2},mathChecksByRound:{2:{right:1,wrong:0}}};assert.deepEqual(summary(p,2),{round:{right:1,wrong:0,unclassified:1},total:{right:1,wrong:0,unclassified:4}});const player=newPlayer({id:'a',name:'A',badges:[]},'A','🍔','#123456'),g=initializeLesson({round:1,phase:'planning'},'supply-demand-v1');pricing(g,player,{id:'1',price:4.5,stock:10});assert.equal(player.mathChecksByRound,undefined);});
