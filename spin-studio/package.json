{
  "name": "spin-studio-classroom-wheel",
  "version": "1.0.0",
  "private": true,
  "description": "A customizable, classroom-ready prize wheel that runs entirely in the browser.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check app.js && node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
